// TODO: Phase 6 — generate 100-300 synthetic transactions across all four leak types
// For now, a placeholder that logs intent

console.log("[synthetic-batch] generator ready — run during Phase 6 to populate test data");
console.log("[synthetic-batch] will generate: payment failures, checkout abandonments, mandate failures, overdue invoices");
